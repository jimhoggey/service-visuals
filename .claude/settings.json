{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] || exit 0; case \"$f\" in /Users/fynnj/Documents/Claude/Projects/ServiceVisualsCreations/*.py) ;; *) exit 0 ;; esac; out=$(/Users/fynnj/Documents/Claude/Projects/ServiceVisualsCreations/.venv/bin/python -m pyflakes \"$f\" 2>&1) || jq -n --arg o \"$out\" '{hookSpecificOutput:{hookEventName:\"PostToolUse\",additionalContext:(\"pyflakes on the file just edited:\\n\" + $o + \"\\nAn undefined name here usually means an edit landed in the wrong function. Fix before continuing.\")}}'; }",
            "timeout": 20,
            "statusMessage": "pyflakes"
          }
        ]
      }
    ]
  }
}
